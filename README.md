# Currency Converter 💱

A simple currency converter web application built using **HTML, CSS, and JavaScript**.

The project uses the **CurrencyAPI** to fetch the latest exchange rates and convert an entered amount from one currency to another.

## ✨ Features

* Convert between different currencies
* Fetch exchange rates using an API
* Form validation for empty and invalid amounts
* Displays the converted amount instantly
* Simple and clean user interface
* Responsive layout

## 🛠️ Technologies Used

* **HTML** – Structure of the webpage
* **CSS** – Styling and layout
* **JavaScript** – Functionality and API handling
* **CurrencyAPI** – Exchange rate data

## 📚 What I Learned

While building this project, I practiced:

* DOM manipulation
* Form submission and `preventDefault()`
* Getting values from input and select elements
* `fetch()`
* `async/await`
* Working with JSON/API responses
* Input validation
* Error handling with `try...catch`

## 📁 Project Structure

```text
Currency Converter/
├── index.html
├── style.css
├── script.js
├── images/
└── README.md
```

## 🔑 API Key

The API key is kept private and is **not included in this repository**.

To run the project locally, you will need your own CurrencyAPI key.

## 🚀 How It Works

1. Enter an amount.
2. Select the currency you want to convert **from**.
3. Select the currency you want to convert **to**.
4. Click **Convert**.
5. JavaScript sends a request to CurrencyAPI.
6. The exchange rate is received and used to calculate the converted amount.
7. The result is displayed on the page.

## 🎯 Project Goal

This project was built as part of my **web development learning journey** to practice working with APIs, asynchronous JavaScript, forms, and DOM manipulation.
